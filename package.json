{
  "name": "static-site",
  "version": "1.0.0",
  "description": "Simple static site with Markdown support",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "node server.js",
    "dev:build": "nodemon --watch src --ext html,css,md,js --ignore dist/ --exec 'npm run build'",
    "dev": "npm run build && npm run serve"
  },
  "dependencies": {
    "express": "^4.18.2",
    "front-matter": "^4.0.2",
    "fs-extra": "^11.1.1",
    "handlebars": "^4.7.8",
    "marked": "^9.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
